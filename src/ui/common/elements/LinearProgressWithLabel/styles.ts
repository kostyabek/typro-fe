import { SxProps } from '@mui/material';

export const mainContainer: SxProps = {
  display: 'flex',
  alignItems: 'center'
};

export const progressContainer: SxProps = {
  width: '100%',
  mr: 1
};

export const labelContainer: SxProps = {
  minWidth: 35
};
